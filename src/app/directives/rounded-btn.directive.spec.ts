import { RoundedBtnDirective } from './rounded-btn.directive';

describe('RoundedBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedBtnDirective();
    expect(directive).toBeTruthy();
  });
});
